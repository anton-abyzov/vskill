# .env.example — populated by the skill author, never committed.
# Copy to .env.local in this directory and fill in real values.
#
# STRIPE_API_KEY: Stripe secret key used by scripts/audit.py to list refunds.
#                 ALWAYS use a test-mode key (prefix sk_test_) outside production.
#                 The integration test will refuse to run against an sk_live_ key.

STRIPE_API_KEY=
